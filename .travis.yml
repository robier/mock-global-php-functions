language: php
php:
  - '7.3'
  - '7.4'
  - '8.0'
  - '8.1'

os:
  - linux

sudo: false

install:
  - composer install

script:
  - composer run test -- --coverage-clover=coverage.xml

after_success:
  - bash <(curl -s https://codecov.io/bash)

cache:
  directories:
    - vendor
